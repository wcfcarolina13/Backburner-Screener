# MEXC API Configuration
# DO NOT commit the real .env file - only this example
# Copy this file to .env and fill in your actual values

# ============================================================
# MEXC Futures API Authentication (Cookie Bypass Method)
# ============================================================
# Since MEXC's official Futures API has been "under maintenance" since 2022,
# we use the browser cookie bypass method.
#
# HOW TO GET YOUR u_id COOKIE:
# 1. Log into MEXC in your browser (futures.mexc.com)
# 2. Open Developer Tools (F12)
# 3. Go to Application tab → Cookies → https://futures.mexc.com
# 4. Find the cookie named "u_id" (value starts with "WEB")
# 5. Copy the entire value and paste below
#
# NOTE: This cookie expires periodically - you'll need to refresh it

MEXC_UID_COOKIE=WEB_your_uid_cookie_here

# ============================================================
# DEPRECATED: Standard API keys (for spot only, futures blocked)
# ============================================================
# MEXC_API_KEY=your_api_key_here
# MEXC_API_SECRET=your_api_secret_here

# Safety Controls
# Set to 'true' to enable live trading (default: false = paper only)
LIVE_TRADING_ENABLED=false

# Maximum position size in USDT (safety limit)
MAX_POSITION_SIZE_USDT=100

# Maximum total exposure in USDT (all positions combined)
MAX_TOTAL_EXPOSURE_USDT=500

# Maximum leverage allowed (will cap bot leverage at this)
MAX_LEVERAGE=20

# Symbols blacklist (comma-separated, won't trade these)
BLACKLISTED_SYMBOLS=

# Emergency stop - set to 'true' to halt all trading
EMERGENCY_STOP=false

# ============================================================
# Render Deployment (Auto Cookie Push)
# ============================================================
# When set, the Chrome extension will automatically push updated
# cookies to your Render deployment after refreshing locally.
#
# HOW TO GET YOUR RENDER API KEY:
# 1. Go to https://dashboard.render.com/account/api-keys
# 2. Click "Create API Key"
# 3. Copy the key (starts with "rnd_")
#
# Your Render service ID (from the dashboard URL)
RENDER_API_KEY=
RENDER_SERVICE_ID=srv-d5jgnker433s73c2q89g
